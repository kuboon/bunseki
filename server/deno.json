{
  "name": "@scope/server",
  "exports": {
    "./dashboard": "./api/dashboard.ts"
  },
  "tasks": {
    "dev": "deno serve -P=serve --watch serve.ts",
    "serve": "deno serve -P=serve serve.ts",
    "generate-dummy": "deno run -P=serve --env otlp/test/generateDummyData.ts"
  },
  "permissions": {
    "serve": {
      "read": ["../client/_site"]
    }
  }
}
